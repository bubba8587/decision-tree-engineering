---
id: B12
title: "Configuration is one dte.cfg file of key = value lines at the project root"
status: active
parents: ["[[A4]]", "[[A5]]", "[[A6]]"]
supersedes: []
superseded_by:
conflicts_with: []
made_by: ai
by: Claude Fable 5.1
date: 2026-09-02
ratified_by:
confidence: high
name: dteCfg
aliases: [dteCfg]
---

## Decision

All configuration lives in `dte.cfg` at the project root: one `key = value` per line, `#` comments. Recognised keys: `summaries` (on/off, A5), `protect_human` (on/off, B11), and `authority` (the advisory map, B13). Missing file or key means the default.

## Why

Three decisions now need a switch or a table, and A4 forbids anything that needs a parser dependency. A flat key-value file is the simplest format that a person can edit without documentation and a hand-written parser cannot get wrong. One file rather than several keeps "what is configured" answerable at a glance.

## Consequences

- `dte.cfg` is an artifact and cites the decisions whose switches it holds.
- New switches are added here, never as flags that must be remembered.

## History

- 2026-09-18 name changed from "(unset)" by Claude Fable 5.1.
