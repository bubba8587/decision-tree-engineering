---
id: B39
name: vendoredRules
title: "An adopter vendors DTE's rule text verbatim and ignored; DTE's decisions are never nodes there"
status: active
parents: ["[[A4]]", "[[A2]]"]
supersedes: []
superseded_by:
conflicts_with: []
made_by: ai
by: Claude Fable 5.1
date: 2026-09-18
ratified_by:
aliases: [vendoredRules]
---

## Decision

DTE's protocol reaches an adopting repo as a verbatim copy of DTE's own files (CLAUDE.md, SPEC.md, ADOPTING.md, README.md) plus one rendered file of DTE's in-effect decisions, all in a directory the adopter's scanner ignores. Each copy names the DTE commit it came from. The adopter's harness loads the copies; its agents never re-author the protocol in their own words, and its tree never holds a node for a DTE rule. The adopter's tree holds only the adopter's decisions.

## Why

An agent told to put the protocol into its agent instructions rewrites it as prose and makes a node of its own for a DTE-usage rule; the intent is a copy of the real text, referenced, never duplicated. A4 makes DTE a package: a package's rules are the package's, with one home. A2 wants every decision recorded once; a second copy in another tree is a decision recorded twice and free to drift. Ignoring the directory matters because the copies cite DTE's IDs, which are not the adopter's (I1). The commit stamp is how an adopter sees the copy is stale.

## Consequences

- `dte vendor --from <DTE checkout>` does the copy, the render, the stamp and the ignore entry (C25).
- The decisions render is one file, so the adopter's vault does not fill with DTE's nodes.
- ADOPTING step 1 and init's next-steps say vendor, not copy.

## History

- 2026-09-18 title changed from "An adopter vendors DTE's rule text verbatim, ignored by its scanner, and never re-creates DTE's decisions as nodes" by Claude Fable 5.1.
- 2026-09-18 title changed from "An adopter vendors DTE's rule text verbatim and ignored; DTE's decisions are never re-created as nodes" by Claude Fable 5.1.
- 2026-09-18 title changed from "An adopter vendors DTE's rule text verbatim and ignored, and never re-creates DTE's decisions as nodes" by Claude Fable 5.1.
- 2026-09-18 references to the feedback log removed; a node holds present reasoning (B41); by Claude Fable 5.1.
- 2026-09-18 adoption history removed from the Why; a node holds present reasoning (B41); by Claude Fable 5.1.
