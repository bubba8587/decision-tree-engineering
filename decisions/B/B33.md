---
id: B33
name: importedNodes
title: "An import re-homes an existing corpus with its properties unchanged; no contest is owed at import"
status: active
parents: ["[[A2]]", "[[A3]]", "[[A7]]"]
supersedes: []
superseded_by:
conflicts_with: []
made_by: ai
by: Claude Fable 5.1
date: 2026-09-18
ratified_by:
confidence: medium
aliases: [importedNodes]
---

## Decision

`dte import` lifts an existing rule corpus into the tree, one markdown file per node. Every property the source carries (made_by, by, date, ratified_by, confidence, contested_by, authorized_by, tags) is written unchanged; nothing is added to the frontmatter. The import writes one History line, `imported from <source>`, and that line is the mark: an imported node owes its one contest (B28) before the first new work under it, not at import. Parents are resolved by id, by name, or by another file in the batch, and ids are allocated parent-first.

## Why

Existing node properties are respected on import so adoption creates no chaos, and properties stay conservative so nodes remain human-manageable. A2 wants every decision recorded; a corpus that exists in prose is already decided and only needs a home. A3 wants provenance kept, so a source that says the owner ruled arrives human-held. A7 wants no re-questioning: lifting 150 rules is not acting under them, so demanding 60 contests whose alternatives were never on the table would be theatre. A History line rather than a field keeps the frontmatter as it was.

## Consequences

- The unratified summary counts imported nodes separately and brief tells subagents what the mark means.
- An unknown property in a source file is an error, never silently dropped or carried.
- A carried ratified_by gets its History line so the outbox does not read it as a vault edit.

## History

- 2026-09-18 references to the feedback log removed; a node holds present reasoning (B41); by Claude Fable 5.1.
- 2026-09-18 adoption history removed from the Why; a node holds present reasoning (B41); by Claude Fable 5.1.
