---
id: C24
name: layerGlobs
title: "specs, tests and agents globs classify citing artifacts; show groups them; scan_self for dogfooding"
status: active
parents: ["[[B37]]", "[[B12]]"]
supersedes: []
superseded_by:
conflicts_with: []
made_by: ai
by: Claude Fable 5.1
date: 2026-09-18
ratified_by:
aliases: [layerGlobs]
---

## Decision

`layer_of(path)` checks the `tests`, `specs`, `agents` and `docs` globs in that order and falls back to code. `show` prints citing artifacts grouped by layer label; `coverage` lists in-effect non-core nodes with no citing test; `scope` counts every layer but docs as reach. The scanner skips the running tool file unless `scan_self = on`, which DTE's own repo sets, and `init` writes the tool's file name into .dteignore.

## Why

B37 wants the layers derived; B12 puts the globs in dte.cfg. During the Solenoid adoption, a vendored tool's own citations point at DTE's tree and failed validate right after init; skipping the running file fixes every adopter at once.

## History

- 2026-09-18 title changed from "dte.cfg specs, tests, agents globs classify citing artifacts; show groups them; scan_self keeps dogfooding" by Claude Fable 5.1.
- 2026-09-18 references to the feedback log removed; a node holds present reasoning (B41); by Claude Fable 5.1.
