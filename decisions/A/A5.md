---
id: A5
title: "A decision is always referred to by ID plus its camelCase name, never by bare ID"
status: active
parents: []
supersedes: []
superseded_by:
conflicts_with: []
made_by: human
by: project owner
date: 2026-09-02
authorized_by: project owner
confidence: high
name: idPlusName
aliases: [idPlusName]
---

## Decision

Whenever a model refers to a decision in chat or in any output meant for a person, it gives the ID together with the node's camelCase `name`, for example A5 idPlusName. A bare "A4" is never sufficient, unless the user has configured summaries off. The tool prints `ID name: title` so both are easy to copy; the name is the chat form, the title is the description a reader opens when the name is not enough.

## Why

DTE is an AI-workflow tool: most of the time a person reads a model's message, the decision file is not open. An ID alone forces a lookup or, more likely, is skimmed past. A short handle keeps the decision legible where it is discussed and is short enough to survive in a code comment (B32); a full sentence was too long to repeat in every line of chat.

## Consequences

- Every node carries a `name` (the handle) and a `title` (the one-sentence description). B32 fixes the shape; B16 the lengths.
- Every tool output that names a node prints `ID name: title`.
- Agent instructions forbid bare IDs in chat and use `ID name`; the title is quoted when the reader needs the sentence.
- A configuration switch may turn summaries off; the default is on.

## History

- 2026-09-02 created by the project owner.
- 2026-09-18 title changed from "A decision is always referred to by ID plus a short dense summary, never by bare ID" by Claude Fable 5.1, authorized by project owner.
- 2026-09-18 name changed from "(unset)" by Claude Fable 5.1, authorized by project owner.
- 2026-09-18 Decision reworded by the owner's ruling: the chat form is ID plus name; scribed by Claude Fable 5.1, authorized by project owner.
- 2026-09-18 adoption history removed from the Why; a node holds present reasoning (B41); by Claude Fable 5.1.
- 2026-09-18 authorized by project owner: adoption history removed from the Why, per the owner.
